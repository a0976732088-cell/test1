name: "proj1"

services:
  backend:
    build:
      context: .
      dockerfile: ./tomcat.dockerfile
      args:
      - "WAR_FILE=labboot-back/target/labboot-back.war"
    # ports:
    #   - 9080:8080
    # container_name: labboot

  frontend:
    build:
      context: .
      dockerfile: ./nginx.dockerfile
      args:
        - "PUBLISH_DIR=labvue-front/dist"
    # ports:
    #   - 9173:80
    # container_name: labvue

  reverse-proxy:
   build:
     context: ./proxy
     dockerfile: ./caddy.dockerfile
   ports:
    - 9000:80
    
